# M.M.I.C._Home

## ローカルサーバーでテストプレイ

このプロジェクトをローカルサーバーで動かしてブラウザで確認できます。

1. ルートフォルダに移動します。
2. `npm install` を実行して依存関係を整備します。
3. `npm run serve` を実行します。
4. ブラウザで `http://localhost:3000/` を開きます。

必要に応じて、`index.html` 以外の HTML を呼び出す場合は URL を変更してください。
